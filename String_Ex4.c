#include<stdio.h>

int main(){
    char str[100], ap[100];
    puts("Digite algo: ");
    fgets(str, sizeof str, stdin);
    for(int i=0; i<strlen(str); i++){
        ap[i]=
    }

    return 0;
}
